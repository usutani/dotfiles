# ls aliases
alias l='ls -CF'
alias la='ls -A'
alias ll='ls -alF'
alias ls='ls -G'

# development aliases
alias g='git'
alias be='bundle exec'
alias rakea='bundle exec rake assets:precompile RAILS_ENV=production'
alias tag='ctags --langmap=RUBY:.rb --exclude="*.js"  --exclude=".git*" -R .'
alias grepruby='find . -name "*.rb" | xargs grep'
alias escapehtml='pbpaste | ruby -r cgi -e "while s=gets; puts CGI.escapeHTML(s.chomp); end" | pbcopy'
alias dssd='find . -name ".DS_Store" -delete'
alias gudp='gud push -c '\''bundle exec rake db:migrate db:seed && bundle exec rails s -p $PORT'\'''

# local alias definitions
if [ -f ~/.bash_aliases ]; then
  . ~/.bash_aliases
fi

# Git
function parse_git_branch {
  git branch --no-color 2> /dev/null | sed -e '/^[^*]/d' -e 's/* \(.*\)/(\1)/'
}
function change_ps1_for_git {
  PS1="\h@\u:\W\$(parse_git_branch) \$ "
}
change_ps1_for_git

# environment variables
export EDITOR=vim
export PATH=/Applications/UpTeX.app/teTeX/bin:/usr/local/bin:$PATH:/usr/local/mysql/bin:/usr/local/share/npm/bin
export LANG=ja_JP.UTF-8
export LSCOLORS=gxfxcxdxbxegedabagacad
alias ctags='/usr/local/Cellar/ctags/5.8/bin/ctags'

# Go
export GOROOT=/usr/local/Cellar/go/1.0.3
export GOPATH=$HOME/_go
export PATH=$PATH:$GOROOT/bin:$GOPATH/bin

# PostgreSQL.app
PATH="/Applications/Postgres.app/Contents/MacOS/bin:$PATH"

# Oracle instant client
source ~/.oracle_client

# Ruby Version Magager
[[ -s "$HOME/.rvm/scripts/rvm" ]] && . "$HOME/.rvm/scripts/rvm" # Load RVM function

# The next line updates PATH for the Google Cloud SDK.
source /Users/usutani/google-cloud-sdk/path.bash.inc

# The next line enables bash completion for gcloud.
source /Users/usutani/google-cloud-sdk/completion.bash.inc
